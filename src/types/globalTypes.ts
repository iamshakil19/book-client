export interface IUser {
  name: string;
  email: string;
}

export interface IBookResponse {
  title: string;
  author: string;
  genre: string;
  publication: string;
  creator: string;
  image: string;
  _id: string;
}
